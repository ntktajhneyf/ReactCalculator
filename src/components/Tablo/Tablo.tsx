
const Tablo: FunctionComponent<PropsWithChildren> = ({children}) => {
    return   <div style={{backgroundColor: "#44014C", width: "300px", minHeight: "200px"}}>{children}</div>
    
}

// const Button: FunctionComponent<PropsWithChildren> = ({children}) => {
//     return <button style={{ borderRadius: '50%' }}>{children}</button>;
//   };

export default Tablo;
